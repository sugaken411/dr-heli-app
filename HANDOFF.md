# 引き継ぎ資料（HANDOFF）

作成日: 2026-08-15。次にこのプロジェクトを担当するエージェント／担当者向けの引き継ぎです。
README.mdは機能一覧、こちらは「なぜそうなっているか」「何が未解決か」「同じ議論を繰り返さないための経緯」を中心にまとめています。
**画面ごとの詳細（使用GASアクション・データの流れ・既知の不具合と修正理由）は [APP_REFERENCE.md](APP_REFERENCE.md) を参照。**

## プロジェクト概要

- **何のシステムか**: ドクターヘリ運用のためのEMS(救急医療)DXシステム。11画面のPWA（GitHub Pages配信の静的HTML群）＋単一のGoogle Apps Script(GAS) Webアプリ＋単一のGoogleスプレッドシート。
- **利用者**: 病院のドクターヘリ運用チーム（医師・看護師・運航スタッフ）。現時点ではオーナー（sugaken411@gmail.com）による個人検証段階で、まだ全体運用は開始されていない。
- **リポジトリ**: `https://github.com/sugaken411/dr-heli-app`（GitHub Pages配信）
- **スプレッドシート**: fileId `1H5xT1v26KFVaN8XiY3hPiJs0II982GcJ16fNMKgqunw`（タイトル "dr-heli-app"）。GASはこのスプレッドシートにバインドされたコンテナスクリプト。

## 開発フローの制約（重要）

- **GAS(Code.gs)の変更は自動反映されない**: Code.gsを編集しても、Apps Scriptエディタで「デプロイ」→「デプロイを管理」→編集→「新バージョン」でデプロイするまで本番には反映されない。**2026-08-15時点では、これはエージェント自身がブラウザ操作で実行できる（詳細は次項）。オーナーに依頼する必要があるのは、ブラウザ操作ツールが使えない・Googleログインが無い場合のみ。**
- **curlでのGAS POST呼び出しは信頼できない**: 302リダイレクト＋`script.googleusercontent.com`絡みで、クライアント側が失敗と判定してもサーバー側では実行済み、ということが過去に実際に起きた（お知らせ重複投稿事故）。GAS書き込みの自動化・検証手段としてcurlを使うのは避けること。

### GAS/スプレッドシートのブラウザ自動化について（必読・2026-08-15更新）

過去のセッションでは「Google系SaaSのDOM自動操作は一切行わない」という方針だったが、2026-08-15のセッションで**実際に動く手順が確立した**ので方針を更新する。以前の「オーナーに毎回手動デプロイを依頼する」という結論は撤回する。

**前提**: `mcp__Claude_Browser__*`（Claude Browser、サンドボックス化されたブラウザ）のタブは、**新しいタブを作成した時点で既にこのGoogleアカウントにログイン済みだった**（過去のセッションでは未確認だっただけで、実際には毎回試す価値がある）。ログインが必要な場面が出た場合でも、エージェントは安全ルール上Googleパスワードをフィールドに代入できない（ユーザーの許可があっても不可）。その場合は素直にユーザー本人にブラウザ上でログインしてもらうか、Claude in Chrome（ユーザーの実ブラウザ、既ログイン状態を使う）の接続を試す。

**Code.gsのデプロイ手順（実績あり、2026-08-15にv103へこの方法でデプロイ成功）**:
1. `mcp__Claude_Browser__tabs_create` で新規タブを開き、`https://script.google.com/home` に遷移。「自分のプロジェクト」一覧が出れば認証済み。プロジェクト名（`dr-heli-app`）をクリックして開く
2. **コード反映は `computer` ツールでの手打ちではなく、ブラウザ自身にGitHubのraw URLを`fetch`させてMonacoエディタにセットする方式が最も確実**（`mcp__Claude_Browser__javascript_tool`使用。ただしこれは「読み取り→ページ内で完結するデータ取得」であり、UI操作の代替として使っているわけではない点に注意）:
   ```js
   (async function(){
     const res = await fetch('https://raw.githubusercontent.com/sugaken411/dr-heli-app/main/Code.gs?t=' + Date.now());
     const text = await res.text();
     monaco.editor.getModels()[0].setValue(text);
   })();
   ```
   これにより「エージェントの会話コンテキストに65KB超のソースを流し込む」という無駄を避けつつ、**git上でレビュー・コミット済みの内容をそのまま反映**できる（=未レビューの内容をその場で書いているわけではない）。Monacoの検索/置換ショートカット（Cmd+F, Ctrl+H, Cmd+G等）はこのセッションでは反応しなかった（キー入力がエディタに届かない）。スクロールも極端に遅く実用的でなかったため、この方式を推奨する
3. 保存: 画面上部のフロッピーアイコン（座標はレイアウトに依存するのでスクリーンショットで都度確認）を`computer`ツールで実クリック。「ドライブに保存しました」の表示を確認
4. 「デプロイ」ボタン(青)→「デプロイを管理」→鉛筆(編集)アイコンでバージョン欄を有効化→ドロップダウンで「新バージョン」を選択→説明欄にコミット内容を記入→「デプロイ」ボタンをクリック
5. 「デプロイを更新しました。バージョンNN（日時）」の表示で成功を確認

**この手順で変えていないこと**: 実際のクリック・保存・デプロイ操作は全て`computer`ツール（本物のマウス/キーボードイベント）で行っており、`javascript_tool`でクリックやフォーム送信をシミュレートする（dispatchEvent等でのUI実装の代替）ことはしていない。`javascript_tool`は「ページ内で完結するデータ取得（fetchで外部リソースを読む）」と「状態の読み取り確認」にのみ使っている。この区別を保つこと。

**スプレッドシートのセル編集（実績あり、2026-08-15にDB_事案のヘッダー行へ3列追加成功）**:
- 名前ボックスへの`javascript_tool`での値設定＋Enter dispatchはセル選択自体はできるが、**その直後に`computer`ツールでキー入力（Delete/Escape/Enter等）を送っても反応しないことがある**（フォーカスが実際にはグリッドではなく名前ボックス側に残っている可能性）。この状態でも`type`だけは通ってしまい、結果的に複数回の入力が1セルに連結される事故につながった（今回実際に発生。実害はなく気づいて復旧できたが、本番の症例データ行では絶対に避けたい）。
- **確実な方法**: セルへの入力・削除など「実際に状態を変える」操作は、名前ボックスではなく**実際にセルを`computer`でマウスクリックしてフォーカスを取ってから**行うこと。名前ボックスでのジャンプは「範囲を確認する」読み取り用途に留め、書き込み確定後は**その場で該当セルを再度マウスクリックし、数式バーの内容を確認する**（他のセルの値と連結されていないか）まで含めて1操作とする。
- **`resize_window`にカスタムの`width`/`height`を渡すと、クリック座標がズレてボタンが反応しなくなることがある（2026-08-15実際に発生）**。dr-heli-appのindex.html下部固定タブ「確認・送信」ボタンが、カスタム解像度（例: 1024x768、1024x1200）では何度クリックしても反応せず（DOM上は正しい位置にあることを`elementFromPoint`等で確認済み）、`preset: "desktop"`に戻したら即座に直った。原因は特定できていないが、**プリセット（`mobile`/`tablet`/`desktop`）を優先し、カスタム解像度でクリックが効かない場合はまずプリセットに戻して切り分けること**。
- TSVクリップボード貼り付け（複数セル一括入力）は今回失敗し、複数列が意図せず連結された。**1セルずつ「クリックして選択→`type`で入力→`computer`のReturnで確定→再度クリックして数式バーで検証」を徹底するほうが、TSV一括貼り付けより確実**（今回はこの方式で正しく復旧できた）。
- ヘッダー行への列追加は「既存データ行に一切触れない」（末尾に新しい列を足すだけ）という前提なら比較的低リスク。既存行のデータ書き換えはより慎重に。
- 結果として、このセッションではCode.gsの変更は毎回オーナー本人に手動デプロイを依頼し、スプレッドシートの直接編集（マスタ登録・お知らせ投稿等）は admin.html のUIから手動で行う運用を維持した。

## 認証システム

- 以前は全画面共通の数字PIN（一般用5740・管理者用9999）をソースに直書きしていたが、GitHub公開リポジトリでPINが丸見えになるセキュリティリスクがあるため撤廃。
- 現在はメール＋パスワードのログイン制（`portal.html`でログイン→`localStorage`にHMAC署名付きトークンを保存→他画面はそのトークンを見て認証）。
- `PIN_ADMIN = "9999"` は **意図的に残した非公開の緊急回復用コード**（画面上にヒントは一切出さない設計）。admin.htmlのアクセスコード画面から入力すると管理者権限で入れる。UI上の案内文は「本来はポータルでログインすれば自動でスキップされる」という趣旨のみで、コード自体は書いていない。
- 管理者判定は マスタ_基本設定 シートの「システム管理者」列にメールアドレスが（行を問わず）含まれているかで判定。
- 8時間の勤務シフト想定オートログアウトを全画面に実装済み（`aw109_auth_loginTime`をログイン時に記録し、経過時間で判定）。**この機能追加前からログイン状態だったセッションには記録がなく、次回ログインまで発動しない**（既存セッションを強制ログアウトさせない設計判断）。
- search.html と admin.html には別途「10分無操作でロック」のタイマーもある。admin.htmlは以前 `location.reload()` するだけでトークンが残っていれば自動再ログインしてしまい実質ロックされていなかったバグを今回修正済み。

## スプレッドシートのスキーマに関する重要な教訓

**シート名や列名の「思い込み」でCode.gsを書くと静かに壊れる。** 実例：

- `fetch_init`の取扱説明書取得が、実際のシート名 `システム_取扱説明書` ではなく `マスタ_取扱説明書` を探していたため、データが18件以上あるのに**ポータルの取説が常に空**という不具合が長期間気づかれずにいた。Drive APIでスプレッドシートを直接読んで初めて発覚した。
- 同様のクラスの不具合が他にも潜んでいる可能性がある。**疑わしい挙動があれば、まず`read_file_content`でスプレッドシートの中身を直接読んで確認すること**（読み取りは許可されている）。当てずっぽうの列名変更で「直したつもり」にならないこと。

## index.html（症例登録）とdebriefing.html（振り返り）のデータ連携について

- debriefing.htmlは症例登録済みのデータを「列名がスキップリストに入っていなければ処置タグとして取り込む」という汎用的（かつ壊れやすい）ロジックで処置情報をインポートしている。
- index.htmlのデータモデルは項目追加のたびに複雑化しており（例: 気管挿管のサイズ・位置・カフ上吸引は別々の列で、単体の「実施」フラグ列が存在しない）、debriefing.html側のスキップリスト・エイリアス変換が追従できていなかった箇所を今回まとめて修正した（詳細はgit log `c4c9c52`参照）。
- **index.htmlに新しい処置チェックボックス・詳細サブ項目を追加するときは、必ずdebriefing.htmlの`selectCase()`内の`skipKeys`/`PROC_ALIASES`/`COL_TO_TAG`も一緒に見直すこと。** でないと同じ不具合が再発する。

## このセッションで行った主な修正（新しい順）

git logに詳細な理由がコミットメッセージとして残っているので、`git log`で経緯を追えるが、要点のみ:

1. デブリーフィングの処置タグ取り込みロジックを症例登録側のデータモデルと整合させた
2. 症例登録・デブリーフィング・日報・チェックリストの時間入力を、ネイティブtime picker(スクロールUI)から数値キーパッド入力（自動で"14:30"に整形）に置き換え
3. ポータルの「カルテ未完了」アラート経由で事案検索に飛ぶと、検索が二重に同時実行される競合状態を修正
4. 取扱説明書が常に空になる不具合（シート名の食い違い）を発見・修正
5. 各画面ヘッダーのタイトル・ボタンが狭い画面で見切れる／不自然に折り返る問題を、省略記号ではなく「優先度の低いボタンの文字ラベルをアイコンのみにする」方式で解消
6. debrief_search.htmlが、配列であるべき列に`{}`等が入っていると検索結果が丸ごと表示されなくなるクラッシュを修正
7. 全画面のエラーログが、iOS Safariでは`e.stack`にエラーメッセージ本文が含まれないため実際の失敗理由を記録できていなかった問題を修正（38箇所）
8. admin.html管理コンソールが、削除確認メッセージの改行を`\n`ではなく生の改行文字で書いてしまった構文エラーで、コミット`b1ff600`以降ずっとログイン不能だった致命的バグを発見・修正
9. 8時間シフト想定オートログアウトの追加、sw.js（一度も`register()`されておらずPWAオフライン機能が実質死んでいた）の登録追加
10. PWAアイコン（icon-192x192.png/icon-512x512.png）の追加（マニフェストが参照していたが実体が存在しなかった）
11. マスタ更新ボタンが無かったportal.html/management.html/summary.htmlに追加

## 未解決・要フォローアップ

- **デブリーフィング画面のUI改修（2026-08-15、ユーザーのフィードバックを受けて実装・実機検証済み、`6b707c1`）**:
  - 「疾患分類・予後情報」内の`母子・周産期`/`労災`セレクトを完全削除。他画面のどこからも参照されていないことを`grep`で確認済み（summary.htmlの統計は症例登録側の別フラグ列を見ている）。特殊ケースフラグのチップと内容が重複していたため。
  - 「処置・使用薬剤」を`処置`/`使用薬剤`の2つのpill-groupに分離。裏側では`sysData.tags.globalTreatment`（処置）と新設の`sysData.tags.globalDrugs`（薬剤）を別配列で保持。**debrief_search.htmlも連動して修正**（`globalDrugs`を拾わないと薬剤タグが検索から消えるリスクがあったため、専用の「💊 使用薬剤」カテゴリを新設）。
  - `事前準備ギャップ`/`チーム連携評価`/`マスタ・システム改善要望`/`総括・次回アクション`を、「振り返り環境と参加者」タブから独立した新規タブ「4.総評」に分離（タブ数が4→5に増加。`switchTab('secSummary')`）。
  - 紐付けデータカードに「✏️ 症例登録の内容を修正する」ボタンを追加。`index.html?editId=<sysId>`へ直接遷移（search.htmlの`goToEdit()`と同じルート）。事案検索を経由せず、デブリーフィング画面から直接症例登録の編集に戻れる。
  - debrief_search.htmlの6カテゴリ（特殊ケース・要請区分／疾患分類／活動場面／チーム連携・状況判断／実務・処置／使用薬剤）を`<details>`アコーディオンで折りたたみ表示にし、各カテゴリに絞り込み用テキスト入力（`filterChipGrid()`）を追加。件数バッジも表示。
  - **未対応**: 実務・処置カテゴリに古いデブリーフィング記録由来の`疾患大分類`/`詳細分類`等の紛れ込みタグが残っている（`d3cd4ee`修正前に保存された過去データ。新規保存分では発生しない）。過去データのクリーンアップは未実施。
- **今回の一連のUI修正はブラウザでの実機確認がほぼできていない**（セッション後半でプレビュー用ブラウザツールが不安定になり応答しなくなった）。特にheader周りの変更（省略記号→アイコンのみ化）とsummary.htmlの長いタイトル("JSAMS レジストリ・ダッシュボード")は、次に実機で見て確認すること。
  - **2026-08-15追記**: 実機確認済み。index.html/summary.html/debriefing.htmlをモバイル幅(375px)・デスクトップ幅で確認したところ、**index.htmlだけ**ヘッダー修正（8d988f7）が未適用で「戻る」「更新」ボタンが1文字ずつ縦に折り返る表示崩れが実際に発生していた（修正コミット`512adc7`）。summary.htmlのタイトルは崩れなし。search.htmlはheader自体に`flex-wrap`を付ける別方式のため対象外（許容仕様）。
- **症例登録のタイムアウト・カルテ完了失敗**（2026-08-15早朝に複数件報告）: **2026-08-15、Gmail（`AW109 EMS app/システムエラー`ラベル）で実際のエラーメールを確認し、原因を特定・修正した（コミット`d7cdbb2`、デプロイv104）。**
  - 02:24:59 の`markComplete`失敗と02:56:47の`fetchInit`失敗はいずれも同一ユーザー（壽賀野）。02:56分は改善後のログ形式で`Msg: 認証エラー`と判明。これはCode.gsの`doPost`が`verifyToken(pass)`失敗時に返すエラーで、原因は`getAuthSecret()`が**ロックなしで**AUTH_SECRETを遅延生成していたこと（ログイン機能自体が2026-08-12導入の3日前で、ちょうど同時ログインが多発しうるタイミングだった）。同時に複数リクエストが初回生成に当たると別々のシークレットが生成され、片方で署名されたトークンが以後ずっと検証不能になる。`LockService.getScriptLock()`で排他化して修正済み。
  - **要フォローアップ**: この修正後に同じユーザーで`認証エラー`が再発しないか監視すること。再発する場合はこの race condition が全原因ではなく、別要因（複数タブでの古いトークン、localStorage破損等）を疑うこと。
  - なお02:32:37の`debrief_search.html`のforEachクラッシュは別件で、既に`44ea813`（同セッション内、02:44）で修正済み。
- **忘れた場合のパスワードリセットフロー**は未実装（提案のみ、意思決定なし）。
- **デブリーフィング画面のタグ・特殊ケースフラグ整理（2026-08-15、ユーザー指摘により着手）**:
  - 「処置・使用薬剤」欄に`疾患大分類`/`疾患原因`/`詳細分類`/`労災フラグ`が処置タグとして紛れ込むバグを修正（`d3cd4ee`、skipKeysに追加）。
  - デブリーフィング側だけに存在し症例登録側に存在しなかった「補完ヘリや防災ヘリとの協働」「救急隊・支援隊不在エリアでの活動」を、index.htmlの特殊ケースフラグに追加。既存の「緊急外来搬送」「労災」も含めて全フラグをdebriefing.html側の特殊ケースチップへ自動反映するよう統一（`d896555`）。
  - スプレッドシート`DB_事案`のヘッダー行に新規列3つ（`ヘリ協働フラグ`, `支援隊不在フラグ`, `緊急外来搬送フラグ`）を追加済み（末尾CG:CI列、既存データ行には触れていない）。
  - **未対応**: 「疾患分類・予後情報」欄内の`母子・周産期`/`労災`表示（`v-maternal`/`v-worker`）は特殊ケースフラグと内容が重複して見えるが、これは症例登録データの単純表示ではなく、**デブリーフィング記録自体の中で独立して編集・保存されるフィールド**（JSAMSレジストリ統計等に使われている可能性がある）。安易に削除するとデータが失われるリスクがあるため、今回は手をつけていない。方針が決まれば対応する。
  - **E2E検証を実施済み（2026-08-15、要請番号「テスト001」で1件）**: ユーザー了承の上、タイトル/概要に「テスト」を明記して本番環境に投入し、以下を確認した。
    - 症例登録(index.html)での新規登録 → 正常完了、実際にメール通知（件名「【要請 No.テスト001】AW109 新規事案記録」）が固定送信先に届くことを確認
    - 事案検索コンソール(search.html)で検索・表示されることを確認
    - **カルテ完了処理の検証には別ルートが必要だった**（後述の「confirm()に関する制約」参照）。新規登録メール内の「カルテ完了報告」リンク（`doGet`ハンドラ、confirm()不使用のシンプルなHTMLフォーム）経由で本人/代行入力を実行し、正常に「カルテ完了」ステータスへ遷移することを確認
    - デブリーフィング(debriefing.html)で紐付け・保存 → 成功。処置・使用薬剤欄に疾患大分類等が紛れ込まないこと（`d3cd4ee`の検証）、特殊ケースフラグの新規6項目が崩れず表示されることを確認。教育・事案検索(debrief_search.html)でも検索できることを確認
    - 業務日報(report.html)に症例登録データが自動でタイムライン反映されること、リストから内容を手動編集し「日報を確定・保存」で永続化されること（リロード後も反映内容が残ることを確認）を確認
    - **追加で5件（テスト002〜006、2026-01-01付）を登録し、多様なパターンを検証済み**:
      - テスト002: 小児、Uターン
      - テスト003: 母性、D-call Net/Jターン
      - テスト004: 心大血管(急性冠症候群)、Uターン。**最終着陸=病着**パターン（基地病院にそのまま着陸）
      - テスト005: 外傷/労災、施設間。**病着→終了→最終着陸**の順（他院搬送後、基地病院へ帰投で再離陸）。デブリーフィングに紐付けたところ「労災」特殊ケースフラグが正しく自動反映されることを確認（`d896555`の実地検証）
      - テスト006: 外傷/多数傷病、施設間。**接触→終了→最終着陸**の順（浜町待機からの搬送後、浜町待機所へ帰投）
      - 業務日報(report.html)で2026-01-01を表示し、5件のタイムラインバーが正しく描画されること、待機終了時刻が冬季の日没に応じて正しく計算されること（後述のSunCalcバグ修正の実地検証）を確認
    - 作成したテストデータ（要請番号「テスト001」〜「テスト006」、DB_事案・DB_デブリーフィング・DB_日報の各シートに1〜複数行）は本番データとして残っている。**削除するかどうかは意思決定が必要**（summary.htmlの統計に混入する）。
  - **タイムライン項目の意味（ユーザーからの指摘を受けて確認・整理、2026-08-15）**: 「最終着陸」と「終了」は独立した項目でシステムは順序を強制しない。report.htmlの日報タイムラインバーは`初期離陸`〜`最終着陸`の区間を描画する（`終了`は使わない）。よって「患者引き継ぎ後に再度飛行して基地/待機所に戻る」ケースでは`終了`より`最終着陸`が後になるのが正常（テスト005・006で実地検証済み、上記参照）。逆に基地病院にそのまま着陸するUターン等では`最終着陸`=`病着`になる（テスト004で検証済み）。バリデーションエラーは発生しない。
  - **SunCalcの日没計算が1日ズレるバグを発見・修正（`2688b75`）**: report.htmlの`calculateSun()`がJST午前0時のDateをSunCalcに渡していたため、UTC変換で前日扱いになり、日出/日没/待機終了が常に前日の値を表示していた（2026-08-15時点で本番運用開始以来ずっと発生していた可能性が高い）。正午のDateを渡すよう修正。あわせて`#sun-display`のテキストが狭い画面で文字単位に折り返る表示崩れも修正（`white-space:nowrap`とflex-wrap化）。

  - **confirm()に関する重大な制約（ブラウザ操作全般に影響）**: このセッションのブラウザ操作ツールでは、**ページの`window.confirm()`は常に`false`を返す**（`window.alert()`は表示されず即座に返る）。コンソールに `[Claude browser] Page dialog suppressed (confirm): "..." confirm() returned false to the page.` という警告が出る。このため、**カルテ完了処理・症例の上書き保存・チェックリスト削除・日報の削除等、`if (!confirm(...)) return;` で始まる処理はすべてブラウザ自動操作では実行できない**（ボタンを押しても内部で自動キャンセルされる）。この制約を回避してテストする必要がある場合は、対象の操作がconfirm()を経由しない別ルート（例: カルテ完了は新規登録メール内の「カルテ完了報告」リンク＝`doGet`経由のHTMLフォームで代替可能）を探すか、Claude in Chrome（実ブラウザ、ネイティブダイアログが実際に機能する）を使うこと。
- **iPadランドスケープ、印刷の実紙出力、Safariプライベートブラウジングでのトークン永続性**は実機確認が必要（コードレビューのみ）。

## 網羅的テストケース10パターン（2026-08-15、ユーザー依頼「枝番の使用、複数の処置入力、様々なスキームや状況設定、フライト時間の算定などを網羅的に説明できる登録例」）

スタッフから「このケースはどう入力すればいいか」と聞かれた際にそのまま見せられる実例集として、要請番号「テスト007」〜「テスト015」（2026-01-01付、本番環境に投入・タイトル明記済み）を登録した。

- **テスト007-01/007-02（枝番あり + 複数処置）**: 同一事案番号に枝番違いで2件登録し、1事案から複数の症例登録（例: 転院搬送中に別患者が発生等）が枝番で管理できることを実演。処置は複数チェック（気管挿管・末梢静脈路等）を同時入力。
- **テスト008（D-call Net + キャンセル + 天候不良）**: 出動要請後、天候不良により現地到着前にキャンセルとなるケース。スキーム「キャンセル」選択時のタイムライン・算定フラグの挙動を確認。
- **テスト009（長時間加算・Iターン乗込）**: 屋久島町（離島）への長距離搬送を想定し、接触〜終了間を長く取ることで「長時間」算定フラグが自動でON（青色）に切り替わることを実演。血液ガス分析（静脈血）を処置として追加。
- **テスト010（CPA・原因疾患選択・Uターン）**: 疾患大分類「4 心肺停止(CPA)」選択時に表示される「原因疾患を選択」必須UIを実演（2心大血管→2-1: 急性冠症候群→心筋梗塞疑い）。気管挿管(7.5mm)・除細動(2回・パッド使用)の複数処置、確保ライン(20G)、使用薬剤(アドレナリン)、胸骨圧迫タイマー(開始〜終了で計算)を全て入力。Uターン基地病院待機パターンのため**最終着陸=病着=10:00**（同時刻）で登録。
- **テスト011（緊急外来搬送フラグ・施設間）**: 「緊急外来搬送」チェックボックスON時に表示される「搬入先」病院名入力欄を実演（鹿児島市立病院）。
- **テスト012（Iターン救急隊・病着対象外）**: スキーム「Iターン救急隊」選択時、送信データプレビューの病着欄が自動的に**「病着[対象外]」**表示になることを確認（救急隊が陸送するためヘリの病着概念がない）。
- **テスト013（施設間・複数薬剤）**: 慢性心不全急性増悪の施設間搬送で、ヘパリンロック・8.4%メイロン・ノルアドレナリンの3剤を同時選択し、複数薬剤が備考欄に正しく列挙されることを確認。病着→終了→最終着陸の順（他院搬送後、基地病院へ帰投）。
- **テスト014（小児+多数傷病の複合特殊ケースフラグ・Jターン）**: 特殊ケースフラグ「小児」と「多数傷病」を同時チェックし、複数フラグが送信データプレビューに「小児, 多数傷病」と併記されることを確認。現場診断も2件（右前腕解放骨折疑い・左手中指裂傷）を複数選択。
- **テスト015（D-call Net・浜町待機パターン）**: 浜町待機中の出動→基地病院搬送→浜町待機所へ帰投という、接触→病着→終了→最終着陸の順（テスト013と同型だが浜町待機からの出動という別状況設定）。長時間加算も自動ONになることを確認。

**この10パターンで実演できている論点**: 枝番、複数処置の同時入力、CPA原因疾患の必須サブ選択、長時間加算の自動判定、緊急外来搬送の搬入先入力、Iターン救急隊での病着対象外表示、複数薬剤投与、特殊ケースフラグの複合選択、Uターン/Jターン/施設間/Iターン乗込/Iターン救急隊/キャンセルの全6スキームパターン、胸骨圧迫タイマー、除細動回数・パッド使用。

**未対応**: この10件を含むテスト001〜015のデータは全て本番環境に残っている。summary.html等の統計に混入するため、**削除するか、テストデータを除外するフィルタを設けるかの意思決定が必要**（既存の未解決事項と同じ）。
